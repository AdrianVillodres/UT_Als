using CodeWithIssues;
namespace TestOfCodeWithIssues
{
    public class TestCodeIssues
    {
        [Theory]
        [InlineData(-1, 0, 0, 0)]
        [InlineData(0, 0, 4, 0)]
        [InlineData(0, 10, 0, 0)]
        public void TC1_ResultZero(int first, int second, int third, int result)
        {
            //Arrange

            //Act
            int sum = Program.SumByMoreOfZero(first, second, third);

            //Assert
            Assert.Equal(result, sum);
        }
        [Theory]
        [InlineData(1, -2, 0, 1)]
        [InlineData(5, 0, 6, 5)]
        [InlineData(7, 0, 0, 7)]
        public void TC2_ResultA(int first, int second, int third, int result)
        {
            //Arrange

            //Act
            int sum = Program.SumByMoreOfZero(first, second, third);

            //Assert
            Assert.Equal(result, sum);
        }
        [Theory]
        [InlineData(5, 3, -4, 8)]
        [InlineData(5, 10, 0, 15)]
        [InlineData(1, 1, -1, 2)]
        public void TC3_ResultAB(int first, int second, int third, int result)
        {
            //Arrange

            //Act
            int sum = Program.SumByMoreOfZero(first, second, third);

            //Assert
            Assert.Equal(result, sum);
        }
        [Theory]
        [InlineData(7, 5, 2, 14)]
        [InlineData(5, 10, 15, 30)]
        [InlineData(6, 2, 4, 12)]
        public void TC4_ResultABC(int first, int second, int third, int result)
        {
            //Arrange

            //Act
            int sum = Program.SumByMoreOfZero(first, second, third);

            //Assert
            Assert.Equal(result, sum);
        }
    }
}
